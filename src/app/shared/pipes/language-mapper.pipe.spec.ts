import { LanguageMapperPipe } from './language-mapper.pipe';

describe('LanguageMapperPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageMapperPipe();
    expect(pipe).toBeTruthy();
  });
});
